#!/bin/sh

set -e

. /usr/share/debconf/confmodule

conf=/etc/mysql/koha-common.cnf
if [ ! -e "$conf" ] && [ ! -L "$conf" ]
then
    ln -s debian.cnf "$conf"
fi
chmod 0600 /etc/koha/wheelers-uplad

#DEBHELPER#

koha-upgrade-schema $(koha-list)

db_stop

exit 0
